# gootyboy.github.io
Deploy to Render:
Build Command: pip install --upgrade pip && python3 -m venv myenv && source myenv/bin/activate && pip install -r app_folder/requirements.txt && pip install gunicorn
